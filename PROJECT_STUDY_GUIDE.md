# NewsCompass「投满分」项目学习文档

## 1. 仓库结论

该 Download ZIP 是 MkDocs 构建后的静态课程站点，包含项目背景、数据分析、随机森林、FastText、BERT、LLM 与模型压缩等 12 个主题页面。它不包含页面引用的 `TMFCode/` 源码目录、训练数据、预训练权重和模型产物，因此不能直接作为可运行源码仓库。

本课程包已经依据页面内容重构了 55–65 号示例脚本，并在 `index.html` 中增加完整项目章节。

## 2. 项目任务

输入新闻标题或短文本，输出以下 10 个类别之一：

`finance, realty, stocks, education, science, society, politics, sports, game, entertainment`

工程上建议增加 `reject`，处理未知领域、乱码、信息不足和边界模糊样本。

## 3. 数据

- `train.txt`：文档描述为 180,000 条，每类 18,000 条。
- `dev.txt`：10,000 条。
- `test.txt`：10,000 条。
- 行格式：`text<TAB>label_id`。
- `class.txt`：类别编号到名称的顺序映射。

必须检查：空行、重复、标签越界、编码、长度分布、类别平衡、跨切分重复和时间泄漏。

## 4. 模型路线

### 4.1 TF-IDF + 传统模型

作为基线验证完整链路。文档随机森林快照约 82.48%。高维稀疏文本还应优先对比 LinearSVC、逻辑回归和朴素贝叶斯。

### 4.2 FastText

字符级默认约 87.61%，字符级 AutoTune 约 91.65%；词级默认日志约 90.79%，词级 AutoTune 约 90.41%。字符级在该短文本场景更稳健，且推理成本低。

### 4.3 BERT

使用 `bert-base-chinese + Linear(768,10)`。核心数据是 `input_ids / attention_mask / labels`。文档日志最高约 93.64%，但复现时需固定完整验证集、随机种子、checkpoint 和 tokenizer。

### 4.4 LLM API

提示词闭集分类快照约 69.08%，平均约 5.31 秒/条。更适合开放集、冷启动、弱标注和低置信度兜底，不适合默认替代固定十分类监督模型。

## 5. 部署

模型应在进程启动时加载一次；接口需要输入校验、版本字段、健康检查、超时、限流、日志和监控。不要使用 Flask debug server 直接承载生产流量。

## 6. 压缩

- 动态量化：主要量化 Linear，仓库快照约 390MB→145MB、F1≈89.26%，结果强依赖硬件和测量方式。
- 知识蒸馏：BERT 教师→BiLSTM 学生，文档记录 104MB，Accuracy 存在 89.89% 与 91.25% 两组不一致快照。
- 剪枝：对 12 层 query 权重做 30% 非结构化剪枝，F1 约 94.07%→91.87%。零权重不等于真实加速。

## 7. 关键审查结果

1. 下载包是静态站点，不含可运行源码和数据。
2. 路径硬编码且大量使用 Windows 路径。
3. BERT 训练 DataLoader 未 shuffle。
4. Tokenizer 未显式限制最大长度。
5. FastText 训练/部署模型命名不一致。
6. 多个 API 示例端口冲突并有复制残留导入。
7. LLM 输出解析和拒识映射不稳健。
8. LLM 评估未真正计算 recall。
9. 蒸馏 KL 散度的 `log_target` 使用错误。
10. 多处指标日志与正文数字不一致。
11. Pickle 模型只能从可信来源加载。
12. 压缩应测质量、文件、内存、延迟和吞吐。

## 8. 推荐复现顺序

1. 数据审计 + 字符 TF-IDF + LinearSVC。
2. FastText 字符/词级四组公平实验。
3. BERT 微调、错误分析和 API。
4. 量化、蒸馏、结构化剪枝并在目标 CPU 上 benchmark。
5. LLM 只作为拒识/疑难样本辅助通道。
