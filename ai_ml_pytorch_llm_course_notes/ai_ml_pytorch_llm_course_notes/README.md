# AI / 机器学习 / PyTorch / NLP / RNN / Transformer 课程笔记

这是一个完全离线的单页课程网站，由上传课件持续整理和扩展。

## 本次新增

- 文本预处理完整模块：NLP 概览、Jieba 分词、词性标注、命名实体、语料分析、n-gram、Padding/Truncation、One-Hot、词频、TF-IDF、Embedding、Word2Vec、FastText、回译增强。
- 循环神经网络完整模块：Embedding 输入、RNN 递推、任务模式、PyTorch 形状、LSTM、GRU、堆叠与双向网络、文本生成项目。
- 12 个新增示例脚本和多个离线动态演示。

## 文件

- `index.html`：课程主页面
- `assets/`：样式与交互脚本
- `examples/`：54 个配套 Python 示例

解压后直接打开 `index.html`。动态演示均使用本地 JavaScript，不依赖网络。
